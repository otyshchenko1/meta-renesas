From 64a420a6374abc8e28885c6ff301c3ef5f901957 Mon Sep 17 00:00:00 2001
From: Petr Nechaev <petr.nechaev@cogentembedded.com>
Date: Mon, 26 Oct 2015 19:32:42 +0300
Subject: [PATCH 1/1] strlen() result cannot be static

---
 pseudo_util.c | 4 ++--
 1 file changed, 2 insertions(+), 2 deletions(-)

diff --git a/pseudo_util.c b/pseudo_util.c
index 16c70e0..b2e8700 100644
--- a/pseudo_util.c
+++ b/pseudo_util.c
@@ -97,8 +97,8 @@ dump_env(char **envp) {
 
 int
 pseudo_has_unload(char * const *envp) {
-	static const char unload[] = "PSEUDO_UNLOAD";
-	static size_t unload_len = strlen(unload);
+	static const char const unload[] = "PSEUDO_UNLOAD";
+	size_t unload_len = strlen(unload);
 	size_t i = 0;
 
 	/* Is it in the caller environment? */
-- 
2.4.3

